<p id="notice"><%= notice %></p>

<h1>Colaboradores</h1>

<table>
  <thead>
    <tr>
      <th>Enólogo</th>
      <th>Cargo</th>
      <th>Revista</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contracts.each do |contract| %>
      <tr>
        <td><%= contract.enologist.name %></td>
        <td><%= contract.position.name %></td>
        <td><%= contract.magazine.name %></td>
        <td><%= link_to 'Ver', contract %></td>
        <td><%= link_to 'Editar', edit_contract_path(contract) %></td>
        <td><%= link_to 'Eliminar', contract, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Agragar Contrato', new_contract_path %>
